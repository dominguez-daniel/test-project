version: 2.1

# commands:
#   print_greeting:
#     parameters:
#       greeting:
#         type: string
#         default: Hello,
#       person_to_greet:
#         type: string
#         default: World
#     steps:
#       - run: echo <<parameters.greeting>> <<parameters.person_to_greet>>

executors:
  node-executor:
    docker:
      - image: circleci/node:16.10.0

jobs:
  build:
    executor: node-executor
    steps:
      - checkout
      - run: npm i
      - run: npm run build
      - run: mkdir -p /tmp/workspace/build
      - persist_to_workspace:
          root: /tmp/workspace
          paths:
            - build/*
      - save_cache:
          key: "npm-packages"
          paths:
            - /node_modules

  test:
    executor: node-executor
    steps:
      - checkout
      - restore_cache:
          keys:
            - "npm-packages"
      - run: npm i
      - run: npm run test

  analyze:
    executor: node-executor
    steps:
      - checkout
      - run: npm i
      - run: npm run analyze
      - attach_workspace:
          at: /tmp/workspace
      - run: echo /tmp/workspace/build/mainbundle.min.js
  
workflows:
  build_test_analyze:
    jobs:
      - build
      - test:
          requires:
            - build
      - analyze:
          requires:
            - test